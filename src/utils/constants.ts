// Default target ID, usually referring to a 3D cube.
export const DEFAULT_TARGET_ID = "cube-3d";

// Default width and height, the typical canvas dimensions.
export const DEFAULT_WIDTH = 500;
export const DEFAULT_HEIGHT = 500;

// Default intensity of light, typically used to adjust the strength of a 3D effect.
export const DEFAULT_INTENSITY = 0.2;

// Default scroll intensity of light. Used to adjust intensity based on scroll events.
export const DEFAULT_SCROLL_INTENSITY = 1.5;

// Default swipe intensity of light. Used to adjust intensity based on swipe events.
export const DEFAULT_SWIPE_INTENSITY = 75;
